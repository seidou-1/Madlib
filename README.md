# Basic simulation to take input value and output that value + additional text

This repo exercises the querySelector method to capture a user's input value and output it with a welcome message text.
